package com.reusserdesign.capacitor_web_auth;

import android.util.Log;

public class CapacitorWebAuth {
    private static final String TAG = "CapacitorWebAuth";
    
    public void log(String message) {
        Log.d(TAG, message);
    }
}
